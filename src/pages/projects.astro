---
// The work page moved to the front door. This keeps the old address alive for
// anything already pointing at it — bookmarks, the admin nav, links out — with
// a permanent redirect rather than a 404.
export const prerender = false;
return Astro.redirect('/', 301);
---
