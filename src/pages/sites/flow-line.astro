---
// Generated by Garage Web - Bold Template
// Original: flowlineplumbing.co.nz
// Generated: 2026-03-12T08:14:18.988Z
export const prerender = true;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Flow Line</title>
    <meta name="description" content="Flow Line" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
      :root {
        --primary: #2563eb;
        --secondary: #1e3a5f;
        --accent: #f59e0b;
        --bg: #ffffff;
        --bg-alt: #f8fafc;
        --text: #1a1a1a;
        --text-muted: #666666;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }

      html { scroll-behavior: smooth; }

      body {
        font-family: 'Space Grotesk', -apple-system, sans-serif;
        background: var(--secondary);
        color: white;
        line-height: 1.5;
        overflow-x: hidden;
      }

      /* Animated background grid */
      .bg-grid {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        z-index: 0;
        opacity: 0.03;
        background-image:
          linear-gradient(var(--primary) 1px, transparent 1px),
          linear-gradient(90deg, var(--primary) 1px, transparent 1px);
        background-size: 60px 60px;
      }

      .container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 32px;
        position: relative;
        z-index: 1;
      }

      /* Navigation */
      nav {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 24px 0;
        background: linear-gradient(to bottom, var(--secondary), transparent);
      }

      nav .container {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .logo {
        display: flex;
        align-items: center;
      }

      .logo-img {
        height: 48px;
        width: auto;
      }

      .logo-text {
        font-weight: 700;
        font-size: 24px;
        letter-spacing: -1px;
      }

      .nav-links {
        display: flex;
        gap: 48px;
        list-style: none;
      }

      .nav-links a {
        color: rgba(255,255,255,0.7);
        text-decoration: none;
        font-size: 15px;
        font-weight: 500;
        transition: color 0.2s;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .nav-links a:hover {
        color: var(--primary);
      }

      .nav-cta {
        background: var(--primary);
        color: var(--secondary);
        padding: 14px 28px;
        text-decoration: none;
        font-weight: 600;
        font-size: 14px;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: all 0.3s;
        clip-path: polygon(0 0, 100% 0, 95% 100%, 5% 100%);
      }

      .nav-cta:hover {
        transform: scale(1.05);
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        display: flex;
        align-items: center;
        padding: 120px 0 80px;
        position: relative;
      }

      .hero-content {
        max-width: 900px;
      }

      .hero-eyebrow {
        color: var(--primary);
        font-size: 14px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 3px;
        margin-bottom: 24px;
        display: flex;
        align-items: center;
        gap: 16px;
      }

      .hero-eyebrow::after {
        content: '';
        flex: 1;
        max-width: 100px;
        height: 2px;
        background: var(--primary);
      }

      .hero h1 {
        font-size: clamp(56px, 10vw, 120px);
        font-weight: 700;
        line-height: 0.95;
        letter-spacing: -4px;
        margin-bottom: 32px;
        text-transform: uppercase;
      }

      .hero h1 .highlight {
        color: var(--primary);
        display: block;
      }

      .hero p {
        font-size: 20px;
        color: rgba(255,255,255,0.7);
        max-width: 500px;
        margin-bottom: 48px;
        line-height: 1.7;
      }

      .hero-buttons {
        display: flex;
        gap: 24px;
      }

      .btn-primary {
        background: var(--primary);
        color: var(--secondary);
        padding: 20px 48px;
        text-decoration: none;
        font-weight: 600;
        font-size: 15px;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: all 0.3s;
        display: inline-flex;
        align-items: center;
        gap: 12px;
        clip-path: polygon(0 0, 100% 0, 95% 100%, 5% 100%);
      }

      .btn-primary:hover {
        transform: translateY(-3px);
        box-shadow: 0 20px 40px rgba(0,0,0,0.3);
      }

      .btn-secondary {
        color: white;
        padding: 20px 48px;
        text-decoration: none;
        font-weight: 600;
        font-size: 15px;
        text-transform: uppercase;
        letter-spacing: 1px;
        border: 2px solid rgba(255,255,255,0.2);
        transition: all 0.3s;
      }

      .btn-secondary:hover {
        border-color: var(--primary);
        color: var(--primary);
      }

      .hero-decoration {
        position: absolute;
        right: 0;
        top: 50%;
        transform: translateY(-50%);
        width: 40%;
        height: 80%;
        background: linear-gradient(135deg, var(--primary) 0%, transparent 70%);
        opacity: 0.1;
        clip-path: polygon(30% 0, 100% 0, 100% 100%, 0% 100%);
        pointer-events: none;
      }

      /* Services */
      .services {
        padding: 120px 0;
        background: var(--bg);
        color: var(--text);
        position: relative;
        clip-path: polygon(0 5%, 100% 0, 100% 95%, 0 100%);
        margin: -40px 0;
        padding: 160px 0;
      }

      .section-header {
        margin-bottom: 64px;
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
      }

      .section-header h2 {
        font-size: clamp(40px, 6vw, 72px);
        font-weight: 700;
        letter-spacing: -2px;
        text-transform: uppercase;
        line-height: 1;
      }

      .section-number {
        font-size: 120px;
        font-weight: 700;
        color: var(--primary);
        opacity: 0.2;
        line-height: 1;
      }

      .services-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 24px;
      }

      .service-card {
        background: var(--bg-alt);
        padding: 40px;
        position: relative;
        overflow: hidden;
        transition: all 0.3s;
        cursor: pointer;
        border-left: 4px solid transparent;
      }

      .service-card:hover {
        border-left-color: var(--primary);
        transform: translateX(8px);
      }

      .service-num {
        font-size: 48px;
        font-weight: 700;
        color: var(--primary);
        opacity: 0.3;
        position: absolute;
        top: 20px;
        right: 20px;
      }

      .service-card h3 {
        font-size: 22px;
        font-weight: 600;
        margin-bottom: 8px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      .service-arrow {
        font-size: 24px;
        color: var(--primary);
        margin-top: 16px;
        transition: transform 0.3s;
      }

      .service-card:hover .service-arrow {
        transform: translateX(8px);
      }

      /* About */
      .about {
        padding: 160px 0;
        position: relative;
      }

      .about-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
      }

      .about-text h2 {
        font-size: clamp(40px, 5vw, 64px);
        font-weight: 700;
        letter-spacing: -2px;
        text-transform: uppercase;
        margin-bottom: 32px;
        line-height: 1;
      }

      .about-text h2 .highlight {
        color: var(--primary);
      }

      .about-text p {
        font-size: 18px;
        color: rgba(255,255,255,0.7);
        line-height: 1.8;
        margin-bottom: 40px;
      }

      .about-features {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 24px;
      }

      .feature {
        background: rgba(255,255,255,0.05);
        padding: 24px;
        border-left: 3px solid var(--primary);
      }

      .feature-value {
        font-size: 36px;
        font-weight: 700;
        color: var(--primary);
      }

      .feature-label {
        font-size: 13px;
        color: rgba(255,255,255,0.5);
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .about-image {
        position: relative;
        height: 500px;
        background: var(--primary);
        clip-path: polygon(10% 0, 100% 0, 100% 100%, 0 100%, 0 10%);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 120px;
      }

      /* Contact */
      .contact {
        padding: 120px 0;
        background: var(--bg);
        color: var(--text);
        clip-path: polygon(0 0, 100% 5%, 100% 100%, 0 100%);
        padding-top: 180px;
      }

      .contact-grid {
        display: grid;
        grid-template-columns: 1.2fr 1fr;
        gap: 80px;
      }

      .contact-info h2 {
        font-size: clamp(40px, 5vw, 64px);
        font-weight: 700;
        letter-spacing: -2px;
        text-transform: uppercase;
        margin-bottom: 24px;
        line-height: 1;
      }

      .contact-info > p {
        color: var(--text-muted);
        font-size: 18px;
        margin-bottom: 48px;
      }

      .contact-details {
        display: flex;
        flex-direction: column;
        gap: 24px;
      }

      .contact-item {
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 24px;
        background: var(--bg-alt);
        border-left: 4px solid var(--primary);
      }

      .contact-icon {
        font-size: 28px;
      }

      .contact-content {
        flex: 1;
      }

      .contact-label {
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 2px;
        color: var(--text-muted);
        margin-bottom: 4px;
      }

      .contact-value {
        font-size: 18px;
        font-weight: 600;
      }

      .contact-cta {
        background: var(--bg-alt);
        padding: 48px;
        text-align: center;
      }

      .contact-cta h3 {
        font-size: 28px;
        font-weight: 700;
        text-transform: uppercase;
        margin-bottom: 16px;
      }

      .contact-cta p {
        color: var(--text-muted);
        margin-bottom: 32px;
      }

      .contact-cta .btn-primary {
        background: var(--primary);
        color: white;
      }

      /* Footer */
      footer {
        padding: 40px 0;
        background: var(--secondary);
        color: white;
      }

      footer .container {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .footer-text {
        font-size: 14px;
        color: rgba(255,255,255,0.5);
      }

      .footer-text a {
        color: var(--primary);
        text-decoration: none;
      }

      /* Countdown */
      .countdown-banner {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: var(--primary);
        color: var(--secondary);
        padding: 16px 32px;
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 24px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-size: 14px;
        z-index: 100;
      }

      .countdown-timer {
        font-family: monospace;
        font-size: 20px;
        font-weight: 700;
      }

      .countdown-btn {
        background: var(--secondary);
        color: var(--primary);
        padding: 10px 24px;
        text-decoration: none;
        font-weight: 700;
        font-size: 13px;
        transition: transform 0.2s;
      }

      .countdown-btn:hover {
        transform: scale(1.05);
      }

      /* Responsive */
      @media (max-width: 968px) {
        .nav-links { display: none; }
        .hero h1 { font-size: 48px; letter-spacing: -2px; }
        .about-content, .contact-grid {
          grid-template-columns: 1fr;
          gap: 48px;
        }
        .about-image { display: none; }
        .services { clip-path: none; margin: 0; padding: 80px 0; }
        .contact { clip-path: none; padding: 80px 0; }
        .section-header { flex-direction: column; align-items: flex-start; gap: 16px; }
        .section-number { font-size: 60px; }
      }
    </style>
  </head>
  <body>
    <div class="bg-grid"></div>

    <nav>
      <div class="container">
        <a href="#" class="logo">
          <span class="logo-text">Flow Line</span>
        </a>
        <ul class="nav-links">
          <li><a href="#services">Services</a></li>
          <li><a href="#about">About</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
        <a href="#contact" class="nav-cta">Get Started</a>
      </div>
    </nav>

    <section class="hero">
      <div class="container">
        <div class="hero-content">
          <div class="hero-eyebrow">Flow Line</div>
          <h1>
            Flow Line
            <span class="highlight">Excellence</span>
          </h1>
          <p>We deliver exceptional results with unmatched dedication. Experience the difference.</p>
          <div class="hero-buttons">
            <a href="#contact" class="btn-primary">
              Get Started
              <span>→</span>
            </a>
            <a href="#services" class="btn-secondary">Explore Services</a>
          </div>
        </div>
      </div>
      <div class="hero-decoration"></div>
    </section>

    <section class="services" id="services">
      <div class="container">
        <div class="section-header">
          <h2>What We Do</h2>
          <span class="section-number">01</span>
        </div>
        <div class="services-grid">
          
        </div>
      </div>
    </section>

    <section class="about" id="about">
      <div class="container">
        <div class="about-content">
          <div class="about-text">
            <h2>
              Why Choose
              <span class="highlight">Flow Line?</span>
            </h2>
            <p>With years of experience and a commitment to excellence, we deliver results that exceed expectations. Our team of dedicated professionals is here to serve you.</p>
            <div class="about-features">
              <div class="feature">
                <div class="feature-value">10+</div>
                <div class="feature-label">Years Experience</div>
              </div>
              <div class="feature">
                <div class="feature-value">500+</div>
                <div class="feature-label">Projects Done</div>
              </div>
              <div class="feature">
                <div class="feature-value">100%</div>
                <div class="feature-label">Satisfaction</div>
              </div>
              <div class="feature">
                <div class="feature-value">24/7</div>
                <div class="feature-label">Support</div>
              </div>
            </div>
          </div>
          <div class="about-image">🏆</div>
        </div>
      </div>
    </section>

    <section class="contact" id="contact">
      <div class="container">
        <div class="contact-grid">
          <div class="contact-info">
            <h2>Let's Talk</h2>
            <p>Ready to start your project? Get in touch and let's make it happen.</p>

            <div class="contact-details">
              

              

              
            </div>
          </div>

          <div class="contact-cta">
            <h3>Ready to Start?</h3>
            <p>Let's discuss your project and see how we can help.</p>
            <a href="tel:#" class="btn-primary">Call Now →</a>
          </div>
        </div>
      </div>
    </section>

    <footer>
      <div class="container">
        <div class="footer-text">© 2026 Flow Line</div>
        <div class="footer-text">Redesigned by <a href="https://garage.co.nz">Garage</a></div>
      </div>
    </footer>

    <div class="countdown-banner">
      <span>Preview Expires In</span>
      <span class="countdown-timer" id="countdown">48:00:00</span>
      <a href="#contact" class="countdown-btn">I Want This</a>
    </div>

    <script>
      const deadline = new Date(Date.now() + 48 * 60 * 60 * 1000);
      function updateCountdown() {
        const diff = deadline - new Date();
        if (diff <= 0) { document.getElementById('countdown').textContent = 'EXPIRED'; return; }
        const h = Math.floor(diff / 3600000);
        const m = Math.floor((diff % 3600000) / 60000);
        const s = Math.floor((diff % 60000) / 1000);
        document.getElementById('countdown').textContent =
          h.toString().padStart(2,'0')+':'+m.toString().padStart(2,'0')+':'+s.toString().padStart(2,'0');
      }
      updateCountdown();
      setInterval(updateCountdown, 1000);
    </script>
  </body>
</html>
